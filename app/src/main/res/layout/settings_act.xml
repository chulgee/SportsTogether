<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    >
    <data>
        <import type="com.iron.dragon.sportstogether.data.LoginPreferences"/>
        <variable
            name="viewModel"
            type="com.iron.dragon.sportstogether.data.viewmodel.SettingsViewModel"/>
    </data>
    <LinearLayout
        android:id="@+id/activity_settings"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingBottom="@dimen/activity_vertical_margin"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:paddingRight="@dimen/activity_horizontal_margin"
        android:paddingTop="@dimen/activity_vertical_margin"
        android:orientation="vertical"
        tools:context="com.iron.dragon.sportstogether.ui.activity.SettingsActivity">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:paddingTop="@dimen/md_content_padding_top"
                android:paddingBottom="@dimen/md_content_padding_bottom"
                android:orientation="horizontal">
            <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:text="@string/buddy_alarm_switch"
                android:layout_weight="1"
                android:textSize="17sp"
                />
            <android.support.v7.widget.SwitchCompat
                    android:id="@+id/buddy_switch"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_weight="0"
                    android:checked="@{LoginPreferences.GetInstance().GetBuddyAlarmOn(context)}"
                    android:onCheckedChanged="@{(view, isChecked) -> viewModel.switchBuddyAlarmChanged(isChecked)}"
                    android:text=""
                     />
            </LinearLayout>
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingTop="@dimen/md_content_padding_top"
            android:paddingBottom="@dimen/md_content_padding_bottom"
            android:orientation="horizontal">
            <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:text="@string/chat_alarm_switch"
                android:layout_weight="1"
                android:textSize="17sp"
                />
            <android.support.v7.widget.SwitchCompat
                android:id="@+id/chat_switch"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_weight="0"
                android:checked="@{LoginPreferences.GetInstance().GetChatAlarmOn(context)}"
                android:onCheckedChanged="@{(view, isChecked) -> viewModel.switchChatAlarmChanged(isChecked)}"
                android:text=""
                />
        </LinearLayout>
    </LinearLayout>
</layout>
